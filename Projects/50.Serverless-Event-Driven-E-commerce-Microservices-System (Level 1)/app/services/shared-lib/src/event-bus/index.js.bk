const { config } = require('dotenv');
config();

let eventBus;

if (process.env.ENVIRONMENT === "aws") {
  eventBus = require('./eventBus.aws.js.bk');
} else {
  eventBus = require('./eventBus.local.js.bk');
}

module.exports = eventBus; 
